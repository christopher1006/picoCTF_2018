Assuming the OS doesn't open the image, use the "file" command to verify type. Image is actually a jpg and requires a change to the
 extension end. Final result is the flag - "picoCTF{extensions_are_a_lie}"
